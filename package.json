{
  "name": "paroquia-nossa-senhora-das-gracas",
  "private": true,
  "version": "1.0.0",
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "predev": "node scripts/ensure-db-tunnel.mjs",
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=paroquia-web",
    "dev:api": "turbo run dev --filter=paroquia-api",
    "dev:local": "node scripts/with-env.mjs USE_LOCAL_DB=1 turbo run dev",
    "db:tunnel": "node scripts/db-tunnel.mjs",
    "db:check": "turbo run db:check --filter=paroquia-api",
    "db:studio": "node scripts/db-studio.mjs",
    "db:studio:local": "node scripts/db-studio.mjs --local",
    "db:sync:prod": "node scripts/db-sync-to-production.mjs",
    "db:heic:deps": "node scripts/install-heic-deps.mjs",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "db:up": "docker compose up -d postgres",
    "db:generate": "npm run prisma:generate --workspace=paroquia-api",
    "db:migrate": "turbo run prisma:migrate --filter=paroquia-api",
    "db:seed": "turbo run prisma:seed --filter=paroquia-api",
    "db:reset": "turbo run prisma:reset --filter=paroquia-api"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
